version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - gofmt
    - goimports
    - errcheck
    - revive
    - ineffassign
    - unused
    - misspell
  settings:
    revive:
      rules:
        - name: var-naming
        - name: exported
