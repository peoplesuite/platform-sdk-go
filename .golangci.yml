run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - staticcheck
    - gofmt
    - goimports
    - errcheck
    - revive
    - ineffassign
    - unused
    - misspell

linters-settings:
  revive:
    rules:
      - name: var-naming
      - name: exported